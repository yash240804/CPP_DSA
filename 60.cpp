#include<iostream>
using namespace std;
int main(){
    int size;
    cin>> size;
    int arr[size];
    // don't do this to decide the size of array this is wrong practice because you are giving variable to size of array.
}